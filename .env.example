# Server-only OpenAI key. Do not use NEXT_PUBLIC_ or commit a real key.
# Endpoint: https://api.openai.com/v1/chat/completions, model: gpt-5.4.
OPENAI_API_KEY=
# Enable only after deployment rate limiting and cost controls are configured.
READING_API_ENABLED=false
